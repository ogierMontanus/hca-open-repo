# Data model

Methodology notes for the entity-centred data model used by this project. These documents distill the conventions developed against the H.C. Andersen diary registers and translate them into a form usable across the wider cultural-entity platform.

The current core schema (tables, fields, relationship vocabulary) lives in [`ai-context/coding_agent_plan.md`](../../ai-context/coding_agent_plan.md) §4. The docs in this folder explain the **rationale** behind it: why Works and Expressions are split the way they are, how the printed-index conventions encode relations, and how to extend the schema when new register types arrive.

## Documents

| File | Purpose |
|---|---|
| [`star-schema.md`](star-schema.md) | Dimensions + facts target model, Power Query / Power Pivot MVP, evolution path from the current CSV layer to the eventual PostgreSQL warehouse |
| [`october-pipeline.md`](october-pipeline.md) | Proposed end-to-end CSV + JSON pipeline for the October mockup — automated `raw/*.xlsx` → `data/normalized/*.csv` → `web/data/*.json` → static site, no SQL/SQLite |
| [`wemi-and-relations.md`](wemi-and-relations.md) | Work / Expression / Manifestation / Item boundary; how `se:` and `Se ogsaa:` markers in printed indexes map to alias and relation tables; the hybrid relational + JSONB design for heterogeneous parsed fields |
| [`source-data-characteristics.md`](source-data-characteristics.md) | How a printed-index entry is structured — parentheses as primary separator, name conventions, multilingual titles, special tokens (`Ͻ:`, `[...]`, guillemets) — with worked examples |
| [`source-field-audit.md`](source-field-audit.md) | Column-by-column fill rates and surfacing status for every field in the three normalized CSVs — spots data that is collected but never shown |
| [`temporal-modelling.md`](temporal-modelling.md) | Entity dates vs. mention dates — why the two date families must be kept separate, and how that distinction flows through filtering, timelines, search, and entity pages |
| [`correspondence-integration.md`](correspondence-integration.md) | Merging the diary indexes with Andersen's letter correspondence (`breve-data/BrevBasen.csv`, sibling checkout). Person-identity crosswalk methodology + quantified results (1,005 confident / 389 ambiguous / 482 unmatched of 1,919 correspondents), a worked Fredrika Bremer example with a triple-verified diary↔letter date match, the `MetaTekst` field's 389 already-curated "Dagbog …" diary citations, confirmed `brev.html?bid=`/`person.html?pid=` link patterns, and why place/artwork data is not yet usable from this source. Exports: [`exports/breve-person-crosswalk-candidates.csv`](exports/breve-person-crosswalk-candidates.csv), [`exports/breve-person-crosswalk-ambiguous.csv`](exports/breve-person-crosswalk-ambiguous.csv) |
| [`collin-place-index.md`](collin-place-index.md) | Digitizing the printed STED-REGISTER (place-name index) from *H. C. Andersens Brevveksling med Edvard og Henriette Collin*, pp. 64–77 — column-order/entry-boundary extraction method (PyMuPDF), two parsing bugs found and fixed via a systematic character-class audit and an alphabetical-order integrity check, four high-confidence OCR name corrections with the alphabetical-position proof behind each, and the printed index's own non-standard sort conventions (V/W grouped, inconsistent Å handling) documented rather than silently "fixed". Deliberately stops short of mapping onto the six-volume letter edition — that is future work. Script: [`../../scripts/correspondence/extract_collin_place_index.py`](../../scripts/correspondence/extract_collin_place_index.py). Output: [`../../data/curated/collin_letters_place_index.csv`](../../data/curated/collin_letters_place_index.csv) (623 rows) + an 11-row human-review queue |
| [`place-toponymy.md`](place-toponymy.md) | Planned: historical place spellings (e.g. *Sverrig*, *Kjøbenhavn*) and modern Danish aliases (e.g. *USA* for *Amerika (de forenede Stater)*) — schema, UI behaviour, and data sources for the `place_alias` table |
| [`place-typology.md`](place-typology.md) | **§K is the current taxonomy (5 categories, GeoNames-letter-based, with up to 3 subcategories each); §A–J are retained as analytical background, not current spec.** §A–D: original bottom-up 11-category breakdown from `data/raw/SV14_places.xml` (481 places). §E: a requested 6-category compression with a GeoNames Feature Class/Code mapping, plus a zero-shot name-pattern test against the larger 2508-place STED-REGISTER (`data/normalized/entities.csv`, ~70% estimated accuracy). §F–I: the (superseded) 6-category variant's GeoNames/Wikidata mappings, `@subtype` markup instruction, a 33-subgroup fine-grained layer, and its own STED-REGISTER estimate (Danish -ø/-sø and -dal manor-name failure modes documented there). §J: a (superseded) 5-category variant that folded water into "Landområder og naturfænomener". §K: the **current** taxonomy — water restored as its own category (1), reordered water→land→buildings→settlements→administrative, each category mapped to explicit GeoNames class letters (H / L+T / S+R / P / A+L.CONT), with up to 3 subcategories per category (categories 4–5 explicitly left unsubdivided, per the source instruction's own assessment that GeoNames' population-size and historical-status splits aren't editorially useful). Validated against SV14 by remapping the existing per-place classification; flags 5 concrete interpretation calls that need confirmation (harbour placement, the park/garden split across categories 2 and 3, landscape-regions moving from "administrative" to "land", the now-obsolete cave and waterfall exceptions) |
| [`place-categorization-copilot-prompt.md`](place-categorization-copilot-prompt.md) | Copilot/bot prompt for classifying place names against the (now twice-superseded) 6-category taxonomy from `place-typology.md` §F — category definitions, boundary-case rules, and a worked 80-place sample with filled-in classifications. Not yet updated to §K's taxonomy |
| [`place-categorization-copilot-report.md`](place-categorization-copilot-report.md) | Archived internal documentation: a copilot-produced classification report for the same 80-place sample, cross-checked against enterprise registers (`Location-V0.91-Data.xlsx`, `HCA-Repository V0.82.xlsx`, `Sted-register.txt`). 78 of 80 places classified with a defensible category; 2 marked uncertain (Falkensten, Chåtdet) |
| [`exports/place-categorization-80cases-and-sv14register.xlsx`](exports/place-categorization-80cases-and-sv14register.xlsx) | Excel workbook implementing `place-typology.md` §G's markup procedure (the retired 6-category scheme, not yet updated to §K's taxonomy): sheet 1 is the verified 80-case sample from the copilot report; sheet 2 is all 481 SV14-register places auto-classified via the GeoNames-class lookup + named exceptions (including an individually reviewed lookup for the 90 non-specific `PPL`-typed entries, after a first mechanical pass over-assigned them to "Bebyggede områder" — see the workbook's "Læsevejledning" sheet for the self-critique note); sheet 3 is aggregate statistics. 2 SV14 places remain flagged `usikker` (Falleberthor, the mismatched Lilienstein record) pending identity resolution |
| [`v0.92-structural-diff.md`](v0.92-structural-diff.md) | Structural diff V0.82 → V0.92 — what changed when the source moved from one workbook to nine (Power Query · FactDim · StarSchema), per-file sheet inventory, and what the pipeline needs in response |
| [`../../scripts/place_typology/`](../../scripts/place_typology/) | Reproducible classification scripts behind `place-typology.md`'s validation numbers: `build_place_excel.py` (§G's 6-category classifier + the 80-case/SV14 Excel export), `subgroup_classify.py` (§H's 33-subgroup layer), `sted_register_subgroup_estimate.py` (§I's STED-REGISTER zero-shot estimate), `kategori_k_mapping.py` (§K's remap of the existing classification to the current 5-category taxonomy). All expect `svNames` checked out as a sibling directory to `hca-open-repo`, or the `SVNAMES_PLACES_XML` env var set |

## Audience

Curators reading raw register entries; coding agents porting parsers; anyone deciding how to extend `entities.csv` or the relation vocabulary.
